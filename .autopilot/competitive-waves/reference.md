# Эталон

На что это должно быть похоже. Собрано со слов пользователя — не выдумано.

Пользователь не называл ни одного образца: задача поставлена указанием на
собственный документ проекта. Режим `full` запрещает самобрифинг эталона —
придуманный образец это подделка чужого вкуса, — поэтому файл пустой намеренно.

Мерой приёмки служит сам бриф и §3 `docs/competitive-plan-2026-09.md`,
а не сравнение с чем-то внешним. Доводка (`polish`) не запрашивалась и не
запускается.
