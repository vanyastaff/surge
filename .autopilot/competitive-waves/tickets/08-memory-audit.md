# 08 — `surge memory audit` — что протухло и что мешало

**Требования:** R21, R21.1
**Blocked by:** 05 · **вторая половина R21 — за 13**
**Зона:** `crates/surge-cli/src/commands/memory.rs` · `crates/surge-persistence/src/memory/`
**Волна:** 2
**Status:** ready

## Что должно заработать

Оператор запускает аудит и видит записи, чей источник изменился с момента проверки, и записи, которые фигурировали в провалившихся и зациклившихся ранах. Аудит предлагает удалить — но не удаляет: это делает человек отдельной командой.

## Из брифа, дословно

> «`surge memory audit` — correlate entries with failed and looping runs, flag staleness against current file hashes, propose pruning»

## Разделы спецификации

Истории 18–19, 45. Решения §7. Границы: `surge-cli`, `surge-persistence`. Швы §1 и §3.

## Критерии приёмки

- [ ] Устаревание считается сравнением текущего хеша файла с записанным в `Provenance`
- [x] Корреляция включает провалившиеся раны
- [ ] **Отложено за таск 13 (`D07`):** корреляция со снятыми guard'ом (зациклившимися) ранами. Признака в схеме нет — guard не построен, а `EscalationRequested` не индексирован и смешивает две причины. Таск 13 обязан оставить долговременный след; после него этот критерий добирается повторным запуском таска 08
- [ ] Аудит **предлагает** пруннинг и ничего не удаляет сам
- [ ] Удаление — отдельная явная команда пользователя
- [ ] `--format json` и человекочитаемый вывод
- [ ] Тесты: устаревшая запись найдена, свежая не тронута, удаления не произошло
- [ ] **Долг из таска 05, зона та же.** Три теста в `crates/surge-persistence/src/memory/store.rs` и `crates/surge-core/src/memory.rs` утверждают только `is_err()`, не природу ошибки: `add_claim_fails_on_duplicate_id`, `insert_claim_with_ignore_policy_*` и `deserializing_verified_status_without_verified_provenance_fails`. Такой тест останется зелёным, если функция начнёт падать по любой другой причине — переименование поля, смена формата хеша. Условие: каждый утверждает **причину** отказа (нарушение уникальности id; `UnprovenVerifiedStatus`, который едет наружу через `de::Error::custom`). Оба ревьюера волны 1 назвали это независимо; у таска 05 дозапросы были исчерпаны

## Исполнение

Агенты Rust Code Studio (R42): `rust-scout` — локация, `rust-builder` — реализация,
`test-engineer` — тесты, `rust-reviewer` — ревью. Гейт: `cargo clippy --workspace
--all-targets --all-features -- -D warnings` + `cargo nextest run` + `cargo fmt` — все зелёные.
Отсутствующая зависимость или инструмент → верни `BLOCKED`, не устанавливай.

## Долг для повторного запуска этого таска (после таска 13)

Половина R21 про зациклившиеся раны ждёт долговременного признака от таска 13.
Когда таск 08 будет перезапущен, вместе с ней закрыть два хвоста, оставшихся с
третьего круга ревью — оба на одну строку каждый, круги ремонта тогда были исчерпаны:

- `windows_drive_letter_path_is_classified_as_a_file_not_a_url` утверждает только
  форму с обратным слэшем. Форма `c:/Users/...` с прямыми слэшами держится
  **конструкцией** фикса (порог в два символа, без опоры на слэш), но утверждением
  не закреплена. Одна строка в тот же тест.
- **Классификатор локатора всё ещё теряет входы молча — и это уже не хвост, а принцип.**
  `file:///path` уходит в `Url` и на устаревание не проверяется, хотя читается локально;
  юникс-путь, первый сегмент которого похож на схему (`my:notes/file.md`, `src:v2/…`),
  туда же. Тот же худший класс, что и починенная буква диска: не ложная находка, а
  тихий пропуск. Добавлять исключения по одному можно бесконечно.
  → Условие: `file:` разворачивается в путь, а для остальных двусмысленных форм решает
  **существование на диске**, а не одна лишь форма строки. Разбор локатора по виду текста
  — код, который ломается на входах, о которых не подумали; проверка диском ошибается
  в сторону «проверил», а не в сторону «молча пропустил».

- `MemoryStore::default_path()` игнорирует `$SURGE_HOME`, в отличие от
  `surge_home_dir()`, которым пользуется реестр ранов. Существующая нестыковка,
  не внесённая этим таском, но живущая ровно в его зоне. Проверить, не ломает ли
  она изоляцию `SURGE_HOME` у демона (v0.2 M4), и либо выровнять, либо записать
  причину расхождения.

## Условия к повторному запуску, добавленные после таска 17 (2026-09-06)

**Блокировка снята.** Признак «ран снят guard'ом» теперь типизирован:
`EscalationCause` в `EventPayload::EscalationRequested`, читается через
`surge_persistence::runs::{read_escalations, is_loop_guard_cause}` — **по полю, а не
по подстроке**. Есть тест-сторож, что MCP и bootstrap не принимаются за guard.
Регулярками по `reason` больше не пользоваться нигде.

**Но цену платишь ты, и она названа заранее — двумя ревьюерами независимо:**

- `read_escalations(&RunReader)` отвечает про **один** ран и делает **полный проход
  лога** — `read_events(EventSeq(0)..EventSeq(u64::MAX))` со свёрткой в Rust, при
  живом индексе `idx_events_kind` (`0001_initial.sql:8`).
- **Кросс-ранового входа нет.** Сигнатура берёт один `RunReader`; реестр
  (`registry.rs`, `RunFilter`/`RunSummary`) не тронут. Вопрос «какие раны сняты
  guard'ом» — во множественном числе — сегодня решается перебором **всех ранов ×
  всех событий каждого**.

→ Условия: (а) фильтр по `kind` уходит в SQL; (б) появляется кросс-рановый вход,
либо в `interfaces.md` явно записано, что перебор — на стороне аудита, чтобы
следующий не наткнулся на это в середине работы.

**Сверено по коду перед перезапуском (2026-09-06): (а) закрыто, не делай его заново.**
`read_escalations` в `crates/surge-persistence/src/runs/escalations.rs` фильтрует
`WHERE kind = 'EscalationRequested'` в SQL по `idx_events_kind` — полного прохода лога
на боевом пути нет. `fold_escalations` рядом — **не** дубль, а чистая эталонная
реализация над готовым срезом, для юнит-тестов атрибуции без базы; так же соотносятся
`query::aggregate_status` и `query::current_status`. Атрибуция ноды — коррелированный
подзапрос к `stage_executions.started_seq`, без переигрывания событий.

**(б) остаётся тебе целиком.** Сигнатура по-прежнему `read_escalations(&RunReader)` —
вопрос **про один ран**. Твой вопрос — во множественном числе. Либо кросс-рановый вход
рядом с реестром, либо явная запись в `interfaces.md`, что перебор ранов живёт на
стороне аудита. Молча обойти все раны и не сказать об этом — не вариант.
